# Copy to .env — defaults target the docker-compose Postgres.
DG_DB_HOST=localhost
DG_DB_PORT=5433
DG_DB_NAME=garage
DG_DB_USER=garage
DG_DB_PASSWORD=garage
# Where raw ingested artifacts (FORScan text, CAN dumps) are stored byte-for-byte.
DG_RAW_DIR=./data/raw
# The one vehicle this instance tracks.
DG_VEHICLE_VIN=1FADP3L94HL223134
